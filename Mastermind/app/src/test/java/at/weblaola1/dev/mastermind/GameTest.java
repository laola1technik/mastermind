package at.weblaola1.dev.mastermind;

public class GameTest {

    /*
    public void should_get_well_places_pegs() {

        HashSet<CodePeg> pegs = new HashSet<>(Arrays.asList(CodePeg.values()));
        CodeGenerator codeGenerator = new CodeGenerator(pegs);
        CodeMaker codeMaker = new CodeMaker();
        CodeBreaker codeBreaker = new CodeBreaker();
        GameSettings settings = new GameSettings(4, pegs);
        Game game = new Game(codeMaker, codeBreaker,settings);
        CompareResult result = game.getWellPlaced();

    }
     */

}
